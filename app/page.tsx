import { Button } from "@/components/ui/button";

const Home = () => {
  return (
    <div className="flex justify-center items-center h-screen">
      <Button className="text-2xl hover:cursor-pointer">Click Me!</Button>
    </div>
  );
};

export default Home;
